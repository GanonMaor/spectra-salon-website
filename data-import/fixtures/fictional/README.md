# Fictional Spectra Pilot Fixture

This directory is intentionally safe for tests and examples.

Rules:

- Do not place real salon, customer, phone, visit, or formula data here.
- Use only fictional names and reserved example phone numbers.
- Real generated import data belongs under `data-import/normalized`, `data-import/mappings`, and `data-import/reports`, which are ignored by Git.
